# تطبيق شاورما السلطان 🌯

نموذج أولي شغّال لتطبيق طلبات المطعم، جاهز للتجربة الآن وقابل للتحويل لـ APK.

## طريقة التجربة فورًا
افتح ملف `index.html` في أي متصفح على الموبايل أو الكمبيوتر — التطبيق هيشتغل كامل بدون سيرفر.

## الملفات
| الملف | الوصف |
|---|---|
| `index.html` | هيكل الصفحة والتصميم (CSS) |
| `app.js` | منطق التطبيق: السلة، الطلبات، التنقل بين الشاشات |
| `data.js` | **المنيو والفروع وأرقام التليفونات** — عدّل هنا لإضافة/تغيير أصناف |
| `promo_data.js` | صورة العرض الترويجي (مضمّنة كـ base64) |
| `كيفية-تحويل-التطبيق-لـ-APK.md` | خطوات تحويل التطبيق لملف APK حقيقي |

## البيانات الحقيقية المستخدمة
✅ اللوجو (تم إعادة رسمه كـ SVG من الصورة المرفوعة)
✅ الفروع: المنصورة وبيلا، بعناوينها
✅ كل أرقام التليفونات
✅ الخط الساخن: 17827
✅ صورة عرض "شاورما كبير + بطاطس كبير" بـ 105 جنيه

## ⚠️ بيانات تجريبية لازم تستبدلها
باقي أصناف المنيو (شاورما لحمة، وجبات، ساندوتشات، إلخ) **بأسعار وأوصاف افتراضية** — مش من عندك. افتح `data.js` وعدّلها بمنتجاتك الحقيقية وأسعارك قبل الاستخدام الفعلي.

## الشاشات المتاحة
- **المنيو**: تصفح الأقسام (شاورما، وجبات، ساندوتشات...) وأضف للسلة
- **العروض**: عروض السلطان الخاصة
- **السلة**: مراجعة الطلب، اختيار توصيل/استلام، إدخال البيانات، تأكيد الطلب
- **تواصل**: اتصال مباشر بالفروع + واتساب
- **الإدارة**: متابعة الطلبات الواردة وتغيير حالتها (قيد الانتظار / جاري التحضير / تم التسليم)

## القيود الحالية
- الطلبات بتتخزن محليًا على كل جهاز (localStorage) — مش متزامنة بين الفروع أو الأجهزة المختلفة. لربط حقيقي محتاج قاعدة بيانات سحابية.
- شاشة الإدارة مفتوحة لأي حد يفتح التطبيق (لا يوجد تسجيل دخول) — للاستخدام الفعلي هتحتاج حماية بكلمة مرور.
